# Reproducible environment for Python 3.12.x.
# NumPy stays on 1.26 because PyTorch 2.2 predates the NumPy 2 ABI.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.2

# Core benchmark and deep-learning models.
torch==2.2.2
pypots==0.3.2
pygrinder==0.7
tsdb==0.8
monai==1.3.2
# PyPOTS probes graph modules at import time; this prevents noisy optional-import errors.
torch-geometric==2.5.3

# Classical ML models.
xgboost==2.1.1

# Data preprocessing.
lasio==0.31
missingno==0.5.2

# Plotting, progress reporting, and bundled notebooks.
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5
jupyterlab==4.2.5
